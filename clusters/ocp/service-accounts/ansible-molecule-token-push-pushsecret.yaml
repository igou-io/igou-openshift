---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: ansible-molecule-token-push
  namespace: service-accounts
spec:
  refreshInterval: 1h
  secretStoreRefs:
    - name: onepassword-sdk-claude
      kind: ClusterSecretStore
  selector:
    secret:
      name: ansible-molecule-token
  data:
    - conversionStrategy: None
      match:
        secretKey: token
        remoteRef:
          remoteKey: hub-ansible-molecule
          property: token
