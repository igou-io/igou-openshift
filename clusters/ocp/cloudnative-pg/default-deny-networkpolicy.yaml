---
# Default-deny (ingress + egress) for namespace cloudnative-pg. Allow policies below
# open exactly the flows observed via NetObserv eBPF sampling. Generated;
# review before merge (see PR body for methodology + caveats).
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny
  namespace: cloudnative-pg
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
