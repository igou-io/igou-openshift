---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: shelfmark
  namespace: shelfmark
spec:
  host: shelfmark.apps.ocp.igou.systems
  to:
    kind: Service
    name: shelfmark
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
