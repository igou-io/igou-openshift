apiVersion: apps/v1
kind: Deployment
metadata:
  name: omnigent
  namespace: omnigent
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: omnigent
  template:
    metadata:
      annotations:
        omnigent.io/sandbox-config-sha256: 02c61c9135eed3120b51910e38f0265c6185485183071b5dbcfc65361a13a656
      labels:
        app: omnigent
    spec:
      serviceAccountName: omnigent-server
      imagePullSecrets:
        - name: quay-local-pull
      securityContext:
        runAsNonRoot: true
      containers:
        - name: omnigent
          image: quay.apps.ocp.igou.systems/igou-io/omnigent-server-openshell@sha256:b50a83b843b0bd2c4b7aa6a05128e7b42cfaf91ee1f1f0168217ce9b555753ad
          ports:
            - name: http
              containerPort: 8000
          envFrom:
            - configMapRef:
                name: omnigent-config
            - secretRef:
                name: omnigent-auth
            - secretRef:
                name: omnigent-machine-client
            - secretRef:
                name: omnigent-opencode-go
            - secretRef:
                name: omnigent-openshell-client
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: omnigent-pg-app
                  key: uri
            - name: OMNIGENT_CONFIG
              value: /etc/omnigent/config.yaml
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - name: artifacts
              mountPath: /data/artifacts
            - name: sandbox-config
              mountPath: /etc/omnigent/config.yaml
              subPath: config.yaml
              readOnly: true
            - name: test-agent
              mountPath: /etc/omnigent/agents
              readOnly: true
            - name: openshell-gateway
              mountPath: /etc/openshell/gateways/ocp/metadata.json
              subPath: metadata.json
              readOnly: true
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: '1'
              memory: 1Gi
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 90
            periodSeconds: 30
            timeoutSeconds: 5
      volumes:
        - name: artifacts
          persistentVolumeClaim:
            claimName: omnigent-artifacts
        - name: sandbox-config
          configMap:
            name: omnigent-sandbox-config
        - name: test-agent
          configMap:
            name: omnigent-test-agent
        - name: openshell-gateway
          configMap:
            name: omnigent-openshell-gateway
