apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: omnigent-runner-nonroot
  namespace: omnigent-sandboxes
rules:
  - apiGroups:
      - security.openshift.io
    resourceNames:
      - nonroot-v2
    resources:
      - securitycontextconstraints
    verbs:
      - use
