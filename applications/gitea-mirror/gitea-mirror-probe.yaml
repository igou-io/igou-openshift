---
# Placeholder - populate via `/add-probe https://gitea-mirror.apps.sno.igou.systems` (skill: add-probe).
# Until then, this file intentionally contains no Probe resources.
# Note: gitea-mirror exposes /api/health for health checks.
