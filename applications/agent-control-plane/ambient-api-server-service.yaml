apiVersion: v1
kind: Service
metadata:
  name: ambient-api-server
spec:
  selector:
    app: agent-control-plane-api
  ports:
    - name: api
      port: 8000
      targetPort: api
    - name: grpc
      port: 9000
      targetPort: grpc
