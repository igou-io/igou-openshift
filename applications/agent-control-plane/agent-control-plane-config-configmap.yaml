apiVersion: v1
kind: ConfigMap
metadata:
  name: agent-control-plane-db-config
data:
  db.host: agent-control-plane-db-rw
  db.port: '5432'
  db.name: ambient
  db.user: ambient
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: agent-control-plane-auth
data:
  acl.yml: |
    - claim: email
      pattern: ^igou\.david@gmail\.com$
