---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: virtualmachine-ops
rules:
# Core VM lifecycle
- apiGroups: ["kubevirt.io"]
  resources: ["virtualmachines", "virtualmachineinstances", "virtualmachineinstancemigrations"]
  verbs: ["*"]
# start/stop/restart/migrate, console/vnc, volume hotplug, guest agent info
- apiGroups: ["subresources.kubevirt.io"]
  resources:
    - virtualmachines/start
    - virtualmachines/stop
    - virtualmachines/restart
    - virtualmachines/migrate
    - virtualmachines/addvolume
    - virtualmachines/removevolume
    - virtualmachines/expand-spec
    - virtualmachines/portforward
    - virtualmachineinstances/console
    - virtualmachineinstances/vnc
    - virtualmachineinstances/portforward
    - virtualmachineinstances/pause
    - virtualmachineinstances/unpause
    - virtualmachineinstances/freeze
    - virtualmachineinstances/unfreeze
    - virtualmachineinstances/softreboot
    - virtualmachineinstances/addvolume
    - virtualmachineinstances/removevolume
    - virtualmachineinstances/guestosinfo
    - virtualmachineinstances/filesystemlist
    - virtualmachineinstances/userlist
  verbs: ["get", "update"]
# DataVolumes; datavolumes/source authorizes cross-namespace clones,
# uploadtokenrequests authorizes virtctl image-upload
- apiGroups: ["cdi.kubevirt.io"]
  resources: ["datavolumes"]
  verbs: ["*"]
- apiGroups: ["cdi.kubevirt.io"]
  resources: ["datavolumes/source"]
  verbs: ["create"]
- apiGroups: ["upload.cdi.kubevirt.io"]
  resources: ["uploadtokenrequests"]
  verbs: ["create"]
# Golden-image DataSources referenced via spec.sourceRef
- apiGroups: ["cdi.kubevirt.io"]
  resources: ["datasources", "dataimportcrons"]
  verbs: ["get", "list", "watch"]
# Snapshots, restores, clones
- apiGroups: ["snapshot.kubevirt.io"]
  resources: ["virtualmachinesnapshots", "virtualmachinerestores"]
  verbs: ["*"]
- apiGroups: ["snapshot.kubevirt.io"]
  resources: ["virtualmachinesnapshotcontents"]
  verbs: ["get", "list", "watch"]
- apiGroups: ["clone.kubevirt.io"]
  resources: ["virtualmachineclones"]
  verbs: ["*"]
# Instance types and preferences referenced by VM specs
- apiGroups: ["instancetype.kubevirt.io"]
  resources:
    - virtualmachineinstancetypes
    - virtualmachineclusterinstancetypes
    - virtualmachinepreferences
    - virtualmachineclusterpreferences
  verbs: ["get", "list", "watch"]
# Namespace/service/PVC visibility for the kubevirt inventory plugin;
# services managed for exposing VM SSH/WinRM endpoints
- apiGroups: [""]
  resources: ["namespaces", "persistentvolumeclaims", "pods"]
  verbs: ["get", "list", "watch"]
- apiGroups: [""]
  resources: ["services"]
  verbs: ["create", "get", "list", "watch", "update", "patch", "delete"]
# cloud-init userdata / sysprep / access-credential payloads for VMs
- apiGroups: [""]
  resources: ["secrets", "configmaps"]
  verbs: ["create", "get", "list", "watch", "update", "patch", "delete"]
