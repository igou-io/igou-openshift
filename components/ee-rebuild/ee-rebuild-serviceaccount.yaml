---
# Identity for the CronJob pod only. The PipelineRun it creates runs
# under the operator-owned `pipeline` SA, which already carries the Quay
# push secret as an imagePullSecret/secret (pac-tenant chart).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ee-rebuild
  annotations:
    argocd.argoproj.io/sync-wave: '0'
