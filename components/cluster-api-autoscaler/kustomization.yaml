apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - cluster-api-autoscaler-system-namespace.yaml
  - cluster-autoscaler-serviceaccount.yaml
  - cluster-autoscaler-rbac.yaml
  - cluster-autoscaler-deployment.yaml
