---
apiVersion: v1
kind: Service
metadata:
  name: cnpg-metrics
  namespace: cloudnative-pg
  labels:
    app.kubernetes.io/name: cloudnative-pg
    app.kubernetes.io/component: metrics
spec:
  selector:
    app.kubernetes.io/name: cloudnative-pg
  ports:
  - name: metrics
    port: 8080
    targetPort: metrics
    protocol: TCP
