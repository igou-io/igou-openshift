---
# Default-deny (ingress + egress) for namespace grafana. Allow policies below
# open exactly the flows observed via NetObserv eBPF sampling. Generated;
# review before merge (see PR body for methodology + caveats).
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny
  namespace: grafana
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
