apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  generation: 1
  name: controller-database
  namespace: aap-external-db
spec:
  cluster:
    name: aap-postgres
  name: controller
  owner: controller