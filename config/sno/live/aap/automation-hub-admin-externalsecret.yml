apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: automation-hub-admin-password
  annotations:
    argocd.argoproj.io/compare-options: IgnoreExtraneous
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: 1password
  target:
    name: automation-hub-admin-password
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: automation-hub-password